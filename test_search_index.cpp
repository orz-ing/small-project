// test_search_index.cpp — 倒排索引单元测试
#include "backend.h"
#include <cassert>
using namespace std;

int main()
{
    // TODO: 测试 SearchIndex
    // 1. 构建3本书的索引 → 验证 buildIndex
    // 2. search("C++") → 验证返回包含"C++"关键词的书
    // 3. search("不存在") → 验证返回空列表
    // 4. addBook / removeBook → 验证增量更新
    // assert(全部通过)
}
